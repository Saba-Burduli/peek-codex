[package]
name = "codex-slice"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "A read-only terminal browser for Codex sessions"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
