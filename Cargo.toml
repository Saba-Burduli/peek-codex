[package]
name = "peek-codex"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "A read-only terminal browser for Codex sessions"

[dependencies]
crossterm = "=0.29.0"
ratatui = "=0.30.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
